import type { Metadata } from 'next'
export const metadata: Metadata = {
  title: 'Unicode Converter Free Online — Text to Unicode Escape',
  description: 'Free online Unicode converter. Convert text to Unicode escape sequences and back. Support for all Unicode characters including emoji and special symbols.',
  keywords: ['unicode converter', 'text to unicode', 'unicode escape', 'unicode encoder', 'unicode decoder'],
}
export default function Layout({ children }: { children: React.ReactNode }) { return children }
