import type { Metadata } from 'next'
export const metadata: Metadata = { title: 'AI Birthday Wish Generator — Birthday Greeting Writer', description: 'Free AI birthday wish generator. Create personalized birthday greetings for friends, family, colleagues, and partners. Multiple options.' }
export default function Layout({ children }: { children: React.ReactNode }) { return children }
