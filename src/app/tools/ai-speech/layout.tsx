import type { Metadata } from 'next'
export const metadata: Metadata = { title: 'AI Speech Writer Free Online — Speech Generator', description: 'Free AI speech writer. Write compelling speeches for weddings, business events, graduation, awards, and any occasion.' }
export default function Layout({ children }: { children: React.ReactNode }) { return children }
