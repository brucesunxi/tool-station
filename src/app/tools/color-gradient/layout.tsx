import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'CSS Gradient Generator Free Online — Color Gradient Maker',
  description: 'Free online gradient generator. Create beautiful CSS gradients with two colors. Choose direction and preview in real-time. Copy CSS code instantly.',
}

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
