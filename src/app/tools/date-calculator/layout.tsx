import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Date Calculator Free Online — Date Difference & Add Days',
  description: 'Free online date calculator. Calculate days between dates or add/subtract days from any date. Get results in years, months, and days.',
}

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
