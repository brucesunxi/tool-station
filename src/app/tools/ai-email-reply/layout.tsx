import type { Metadata } from 'next'
export const metadata: Metadata = { title: 'AI Email Reply Generator — Smart Reply Assistant', description: 'Free AI email reply generator. Draft professional email replies quickly. Choose from professional, friendly, or formal tones.' }
export default function Layout({ children }: { children: React.ReactNode }) { return children }
