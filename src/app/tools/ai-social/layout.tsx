import type { Metadata } from 'next'
export const metadata: Metadata = { title: 'AI Social Media Post Generator — Content Creator', description: 'Free AI social media post generator. Create engaging content for Twitter, LinkedIn, Instagram, Facebook, and TikTok. Includes hashtags.' }
export default function Layout({ children }: { children: React.ReactNode }) { return children }
