import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'PDF Splitter Free Online — Extract Pages From PDF',
  description: 'Free online PDF splitter. Extract specific pages from PDF files. Choose individual pages or ranges. Download the extracted pages as a new PDF document.',
}

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
