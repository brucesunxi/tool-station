import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'PDF Password Protector Free Online — Add or Remove PDF Passwords',
  description: 'Free online PDF password tool. Add password protection to your PDF files or remove existing passwords. Keep your documents secure.',
}

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
