import type { Metadata } from 'next'
export const metadata: Metadata = {
  title: 'Text to Slug Converter Free Online — URL Slug Generator',
  description: 'Free online slug generator. Convert any text to clean, SEO-friendly URL slugs. Supports lowercase, hyphen-separated, and custom separator options.',
  keywords: ['slug generator', 'text to slug', 'URL slug', 'SEO slug', 'slug converter online'],
}
export default function Layout({ children }: { children: React.ReactNode }) { return children }
