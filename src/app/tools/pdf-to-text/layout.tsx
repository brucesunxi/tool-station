import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'PDF to Text Extractor Free Online — Extract Text From PDF',
  description: 'Free online PDF text extractor. Extract all text content from PDF files. Perfect for copying text from scanned documents, reports, and academic papers.',
}

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
